struct ContentView: View {
    var body: some View {
        Text("Hello, World!")
            .foregroundColor(.blue) // Apply a modifier to change the text color
            .padding() // Apply a modifier to add padding around the text
    }
}
